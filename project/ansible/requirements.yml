---
# Ansible Galaxy requirements for PayWallet deployment
collections:
  - name: community.general
    version: ">=7.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.crypto
    version: ">=2.0.0"

# Python requirements (if needed)
# - name: requests
#   version: ">=2.25.0"
